<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>OOO</title>

    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css" integrity="sha384-BVYiiSIFeK1dGmJRAkycuHAHRg32OmUcww7on3RYdg4Va+PmSTsz/K68vbdEjh4u" crossorigin="anonymous">

    <script src="//ajax.googleapis.com/ajax/libs/jquery/1.11.0/jquery.min.js"></script>
    <script src="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/js/bootstrap.min.js" integrity="sha384-Tc5IQib027qvyjSMfHjOMaLkfuWVxZxUPnCJA7l2mCWNIpG9mGCD8wGNIcPD7Txa" crossorigin="anonymous"></script>

    <style>
      .navbar-default .navbar-nav>li>a {
        font-size: 20px;
      }
      .jumbotron{
        margin: 5vh auto;
      }
      img {
        width: 100px;
        height: 100px;
      }
      .navbar-default .navbar-nav>li>a #id  {
        float: right;
      }

      .plan{
        padding: 1vw;
        white-space: normal;
      }
      .plan:hover{
        background-color: lightgrey;
      }
      .plan:click{
        background-color: grey;
      }

    </style>

  </head>
  <body>
    <div class="navbar navbar-inverse">
      <div class="container-fluid">
        <div class="navbar-header">
          <div class="navbar-brand">Object Oriented Outings
          </div>
            <ul class="nav navbar-nav">
              <li><a href="/">Home</a></li>
              <% if current_user %>
                <li><a href="<%= user_path(current_user) %>"><%= current_user.name %></a></li>
                <li class = 'dropdown'>
                  <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">Locations<span class="caret"></span></a>
                  <ul class='dropdown-menu'>
                  <% Location.all.sort.each do |location| %>
                    <li><%= link_to location.name, location_path(location) %></li>
                  <% end %>
                  </ul>
                </li>
                <li><a href="/logout">Logout</a></li>
              <% else %>
                <li><a href="/login">Login</a></li>
                <li><a href="/logup">Logup</a></li>
              <% end %>
            </ul>
          </div>
        </div>
      </div>
    </div>
    <%= yield %>
  </body>
</html>
